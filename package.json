{
  "name": "react-native-styles-shorthand",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build:module": "node_modules/.bin/babel src --out-dir build --presets es2015",
    "build:browser": "browserify build/index.js -o build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0"
  }
}
